name: what_todo
description: A simple todo app in flutter.

publish_to: 'none' # pub.dev'de yayınlamak istiyorsanız bu satırı kaldırın

version: 1.0.0+1

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  google_fonts: ^1.1.0

  sqflite:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter
  



flutter:

  uses-material-design: true

  assets:
    - assets/images/